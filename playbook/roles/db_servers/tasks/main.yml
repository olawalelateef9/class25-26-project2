---
- name: Install PostgreSQL client and development headers
  dnf:
    name: 
      - postgresql15
      - postgresql15-devel
      - python3-pip
      - gcc
      - python3-devel
    state: present

- name: Install psycopg2 via pip (needed for Ansible postgresql modules)
  pip:
    name: psycopg2-binary