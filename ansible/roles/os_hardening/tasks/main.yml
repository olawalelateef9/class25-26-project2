---
- name: Update only security packages
  dnf:
    name: '*'
    security: yes
    state: latest
    update_cache: yes

- name: Disable SSH password authentication
  lineinfile:
    path: /etc/ssh/sshd_config
    regexp: '^PasswordAuthentication'
    line: 'PasswordAuthentication no'
    state: present
  notify: restart sshd

- name: Ensure SSH is allowed in Firewalld (BEFORE starting service)
  firewalld:
    service: ssh
    permanent: yes
    state: enabled
    immediate: yes  # This is the key—it applies it to the running config

- name: Start and enable Firewalld
  service:
    name: firewalld
    state: started
    enabled: yes

- name: Allow SSH and App Port (8000)
  firewalld:
    port: "{{ item }}/tcp"
    permanent: yes
    state: enabled
    immediate: yes
  loop:
    - '22'
    - '8000'

- name: Force connection reset
  meta: reset_connection
     

- name: Wait for SSH to be available again
  wait_for_connection:
    delay: 5
    timeout: 60

